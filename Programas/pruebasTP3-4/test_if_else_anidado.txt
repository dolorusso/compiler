@ Programa valido. If anidado.
A
{
    long A.X;
    A.X := 3L;

    if (A.X >= 3L){
        print(&if&);
        if (A.X >= 2L)
          print(&ifif&);
        else
           print(&elseelse&);
        endif;
    } else
        print(&else&);

    endif;
}