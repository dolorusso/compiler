A
{
    long A.X;
    do {
        A.X := 1;
        do {
            A.X := 1;
            if ( 4L > 3L){
                print(&if&);
            } else {
                print(&else&);
            } endif;
        } until ( A.X < 3L);
    } until ( A.X < 3L);

    print(&AFUERA&);
}